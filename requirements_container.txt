# ============================================================
# Restaurant Language Unit – Container dependencies
# ============================================================
# Pinned for Python 3.8 / Ubuntu 20.04 / ROS Noetic
#
# Install inside the container with:
#   pip3 install --no-cache-dir -r requirements_container.txt
#
# NOTE: av==10.0.0 has no cp38 wheel, so it builds from source.
#       --no-build-isolation is REQUIRED (container's Cython 0.29
#       is compatible; the isolated build pulls Cython 3.x which
#       fails on av's older .pyx files).
#
# System prerequisites (run once):
#   sudo apt-get update && sudo apt-get install -y portaudio19-dev
# ============================================================

# --- STT engine dependencies, ordered for correct resolution ---
# tokenizers pinned first so huggingface-hub doesn't pull >=0.20
tokenizers==0.15.2
huggingface-hub==0.20.3

# ctranslate2 3.x line (last supporting Python 3.8 cleanly)
ctranslate2==3.24.0

# faster-whisper 0.10.x (avoids breaking API changes in 1.x)
faster-whisper==0.10.1

# av must be installed WITH --no-build-isolation (see note above)
# If installing manually:  pip3 install --no-cache-dir --no-build-isolation av==10.0.0

# --- Microphone capture ---
# pyaudio wraps PortAudio. Requires portaudio19-dev (apt).
PyAudio==0.2.14

# --- Text-to-Speech (robot voice, optional) ---
pyttsx3==2.90

# --- Coloured terminal output ---
colorama==0.4.6
